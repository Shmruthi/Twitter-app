<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

<%= form_for(current_user.relationships.
                          find_by_followed_id(@user), :html => { :method => :delete},
                          :render => true ) do |f| %>
 
  <div class="actions"><%= f.submit "UnFollow" %></div>

<% end %>

